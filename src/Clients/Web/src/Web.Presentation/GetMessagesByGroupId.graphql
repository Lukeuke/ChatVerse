query GetMessagesByGroupId($groupId: UUID!){
    messagesByGroupId(groupId: $groupId, order_by: {timeStampOffset: ASC}){
        nodes {
            content,
            senderFullName
        }
    }
}